[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "22"

# React Router kliens oldali útvonalak (pl. /rolam) a buildelt index.html-re essenek.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
